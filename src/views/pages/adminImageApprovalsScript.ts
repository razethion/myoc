export const adminImageApprovalsScript =
    "(function () {\n    const root = document.querySelector('[data-image-approvals]');\n    const dataScript = document.getElementById('image-approval-data');\n\n    if (!root || !dataScript) {\n        return;\n    }\n\n    const currentContainer = root.querySelector('[data-approval-current]');\n    const sidebarContainer = root.querySelector('[data-approval-sidebar]');\n    const csrfToken = root.dataset.csrfToken || '';\n    let state = JSON.parse(dataScript.dataset.imageApprovalState || '{}');\n    let selectedActions = {};\n\n    const sfwActions = [\n        ['approve_sfw_homepage', 'Approve SFW, Allow Homepage'],\n        ['approve_sfw_no_homepage', 'Approve SFW, No Homepage'],\n        ['mark_nsfw', 'Mark as NSFW'],\n        ['report_sfw', 'Report'],\n    ];\n    const nsfwActions = [\n        ['approve_nsfw', 'Approve NSFW'],\n        ['mark_sfw_homepage', 'Mark SFW, Approve Allow Homepage'],\n        ['mark_sfw_no_homepage', 'Mark SFW, Approve No Homepage'],\n        ['report_nsfw', 'Report'],\n    ];\n\n    function escapeHtml(value) {\n        return String(value == null ? '' : value)\n            .replace(/&/g, '&amp;')\n            .replace(/</g, '&lt;')\n            .replace(/>/g, '&gt;')\n            .replace(/\"/g, '&quot;')\n            .replace(/'/g, '&#39;');\n    }\n\n    function formatBytes(value) {\n        if (!Number.isFinite(Number(value))) {\n            return 'Unknown size';\n        }\n\n        const bytes = Number(value);\n        const units = ['B', 'KB', 'MB', 'GB'];\n        let size = bytes;\n        let unitIndex = 0;\n\n        while (size >= 1024 && unitIndex < units.length - 1) {\n            size /= 1024;\n            unitIndex += 1;\n        }\n\n        return `${size.toFixed(unitIndex === 0 ? 0 : 1)} ${units[unitIndex]}`;\n    }\n\n    function formatDimensions(variant) {\n        if (!variant || !variant.width || !variant.height) {\n            return 'Unknown dimensions';\n        }\n\n        return `${variant.width} x ${variant.height}`;\n    }\n\n    function formatDate(value) {\n        if (!value) {\n            return 'Never';\n        }\n\n        const date = new Date(String(value).replace(' ', 'T') + 'Z');\n\n        if (Number.isNaN(date.getTime())) {\n            return value;\n        }\n\n        return date.toLocaleString();\n    }\n\n    function formatPendingCount() {\n        const count = Number.isFinite(Number(state.pendingCount)) ? Number(state.pendingCount) : state.pending.length;\n\n        return String(count);\n    }\n\n    function actionLabel(action) {\n        const found = sfwActions.concat(nsfwActions).find((item) => item[0] === action);\n        return found ? found[1] : action;\n    }\n\n    function getMovedVariant(rating) {\n        if (!state.current) {\n            return null;\n        }\n\n        if (rating === 'sfw' && selectedActions.nsfw === 'mark_sfw_homepage' && !state.current.sfw) {\n            return {...state.current.nsfw, rating: 'sfw', movedFrom: 'NSFW'};\n        }\n\n        if (rating === 'sfw' && selectedActions.nsfw === 'mark_sfw_no_homepage' && !state.current.sfw) {\n            return {...state.current.nsfw, rating: 'sfw', movedFrom: 'NSFW'};\n        }\n\n        if (rating === 'nsfw' && selectedActions.sfw === 'mark_nsfw' && !state.current.nsfw) {\n            return {...state.current.sfw, rating: 'nsfw', movedFrom: 'SFW'};\n        }\n\n        return null;\n    }\n\n    function sourceWillMove(rating) {\n        return (rating === 'sfw' && selectedActions.sfw === 'mark_nsfw')\n            || (rating === 'nsfw' && (\n                selectedActions.nsfw === 'mark_sfw_homepage'\n                || selectedActions.nsfw === 'mark_sfw_no_homepage'\n            ));\n    }\n\n    function renderVariant(rating) {\n        const current = state.current;\n        const original = current && current[rating];\n        const moved = getMovedVariant(rating);\n        const variant = moved || original;\n        const title = rating === 'sfw' ? 'SFW Image' : 'NSFW Image';\n        const actions = rating === 'sfw' ? sfwActions : nsfwActions;\n        const selected = selectedActions[rating] || '';\n        const oppositeOccupied = rating === 'sfw'\n            ? Boolean(current && current.nsfw)\n            : Boolean(current && current.sfw);\n        const moveWouldConflict = (selected === 'mark_nsfw' || selected === 'mark_sfw_homepage' || selected === 'mark_sfw_no_homepage')\n            && oppositeOccupied;\n\n        if (!variant || sourceWillMove(rating)) {\n            const moveMessage = sourceWillMove(rating)\n                ? `<p class=\"text-sm text-warning\">${title} will move to ${rating === 'sfw' ? 'NSFW' : 'SFW'} on submit.</p>`\n                : '<p class=\"text-sm text-base-content/60\">None</p>';\n\n            return `\n                <section class=\"admin-approval-panel rounded border border-base-300 bg-base-200 p-4\">\n                    <div class=\"mb-3 flex items-center justify-between gap-3\">\n                        <h3 class=\"text-lg font-bold\">${title}</h3>\n                        <span class=\"badge\">${rating.toUpperCase()}</span>\n                    </div>\n                    <div class=\"flex min-h-80 items-center justify-center rounded bg-base-300/40 p-6 text-center\">\n                        ${moveMessage}\n                    </div>\n                    ${original ? renderActions(rating, actions, selected, moveWouldConflict) : ''}\n                </section>\n            `;\n        }\n\n        return `\n                <section class=\"admin-approval-panel rounded border border-base-300 bg-base-200 p-4\">\n                <div class=\"mb-3 flex items-center justify-between gap-3\">\n                    <h3 class=\"text-lg font-bold\">${title}</h3>\n                    <span class=\"badge ${variant.needsReview ? 'badge-warning' : 'badge-success'}\">${variant.needsReview ? 'Pending' : escapeHtml(variant.reviewStatus)}</span>\n                </div>\n                ${moved ? `<div class=\"alert alert-info mb-3 py-2 text-sm\">Moved from ${escapeHtml(moved.movedFrom)} for preview. Submit to save it.</div>` : ''}\n                ${moveWouldConflict ? '<div class=\"alert alert-error mb-3 py-2 text-sm\">Move target already has an image.</div>' : ''}\n                <a class=\"block overflow-hidden rounded bg-base-300\" href=\"${escapeHtml(variant.imageUrl)}\" rel=\"noopener noreferrer\" target=\"_blank\">\n                    <img alt=\"${title}\" class=\"block max-h-[34rem] w-full object-contain\" src=\"${escapeHtml(variant.imageUrl)}\">\n                </a>\n                <dl class=\"mt-4 grid gap-2 text-sm\">\n                    <div><dt class=\"font-semibold\">Artist</dt><dd>${escapeHtml(variant.artist || 'Unknown')}</dd></div>\n                    <div><dt class=\"font-semibold\">Size</dt><dd>${escapeHtml(formatBytes(variant.byteSize))}</dd></div>\n                    <div><dt class=\"font-semibold\">Dimensions</dt><dd>${escapeHtml(formatDimensions(variant))}</dd></div>\n                    <div><dt class=\"font-semibold\">R2 Path</dt><dd class=\"break-all font-mono text-xs\">${escapeHtml(variant.objectKey)}</dd></div>\n                    <div><dt class=\"font-semibold\">Approved At</dt><dd>${escapeHtml(formatDate(variant.approvedAt))}</dd></div>\n                </dl>\n                ${original ? renderActions(rating, actions, selected, moveWouldConflict) : ''}\n            </section>\n        `;\n    }\n\n    function renderActions(rating, actions, selected, moveWouldConflict) {\n        return `\n            <fieldset class=\"mt-4 grid gap-2\">\n                ${actions.map(([value, label]) => `\n                    <label class=\"btn justify-start ${selected === value ? 'btn-warning' : 'btn-outline'}\">\n                        <input class=\"sr-only\" data-action-input=\"${rating}\" name=\"${rating}Action\" type=\"radio\" value=\"${value}\" ${selected === value ? 'checked' : ''}>\n                        ${escapeHtml(label)}\n                    </label>\n                `).join('')}\n                ${moveWouldConflict ? '<p class=\"text-sm text-error\">Choose a different action before submitting.</p>' : ''}\n            </fieldset>\n        `;\n    }\n\n    function renderCurrent() {\n        if (!currentContainer) {\n            return;\n        }\n\n        if (!state.current) {\n            currentContainer.innerHTML = `\n                <div class=\"flex min-h-[28rem] items-center justify-center rounded border border-dashed border-base-300 bg-base-200 p-8 text-center\">\n                    <div>\n                        <h2 class=\"text-2xl font-bold\">No images need approval</h2>\n                        <p class=\"mt-2 text-sm text-base-content/70\">Pending uploads will appear here.</p>\n                    </div>\n                </div>\n            `;\n            return;\n        }\n\n        const item = state.current;\n        currentContainer.innerHTML = `\n            <div class=\"mb-5 flex flex-col gap-4 lg:flex-row lg:items-start lg:justify-between\">\n                <div class=\"min-w-0\">\n                    <h2 class=\"text-2xl font-bold\">Image Approvals</h2>\n                    <p class=\"mt-1 text-sm text-base-content/70\">Oldest upload first</p>\n                </div>\n                <button class=\"btn btn-primary\" data-submit-approval type=\"button\">Submit</button>\n            </div>\n            <section class=\"mb-5 grid gap-3 rounded border border-base-300 bg-base-200 p-4 text-sm md:grid-cols-2 xl:grid-cols-4\">\n                <div><p class=\"font-semibold\">Uploader</p><p><a class=\"admin-approval-link\" href=\"${escapeHtml(item.user.profileUrl)}\">@${escapeHtml(item.user.username)}</a></p><p class=\"break-all text-xs text-base-content/60\">${escapeHtml(item.user.email)}</p></div>\n                <div><p class=\"font-semibold\">User ID</p><p class=\"break-all font-mono text-xs\">${escapeHtml(item.user.id)}</p></div>\n                <div><p class=\"font-semibold\">Character</p><p><a class=\"admin-approval-link\" href=\"${escapeHtml(item.character.url)}\">${escapeHtml(item.character.name)}</a></p><p class=\"break-all font-mono text-xs text-base-content/60\">${escapeHtml(item.character.id)}</p></div>\n                <div><p class=\"font-semibold\">Uploaded</p><p>${escapeHtml(formatDate(item.createdAt))}</p><p class=\"text-xs text-base-content/60\">Updated ${escapeHtml(formatDate(item.updatedAt))}</p></div>\n            </section>\n            <div class=\"admin-approval-image-grid\">\n                ${renderVariant('sfw')}\n                ${renderVariant('nsfw')}\n            </div>\n            <div class=\"mt-5 flex justify-end\">\n                <button class=\"btn btn-primary\" data-submit-approval type=\"button\">Submit</button>\n            </div>\n        `;\n\n        for (const input of currentContainer.querySelectorAll('[data-action-input]')) {\n            input.addEventListener('change', () => {\n                selectedActions[input.dataset.actionInput] = input.value;\n                renderCurrent();\n            });\n        }\n\n        for (const button of currentContainer.querySelectorAll('[data-submit-approval]')) {\n            button.addEventListener('click', submitApproval);\n        }\n    }\n\n    function renderSidebar() {\n        if (!sidebarContainer) {\n            return;\n        }\n\n        sidebarContainer.innerHTML = `\n            <section>\n                <div class=\"mb-3 flex items-center justify-between\">\n                    <h3 class=\"font-bold\">Pending</h3>\n                    <span class=\"badge\">${escapeHtml(formatPendingCount())}</span>\n                </div>\n                <div class=\"grid max-h-[42vh] gap-2 overflow-y-auto pr-1\">\n                    ${state.pending.length ? state.pending.map((item) => `\n                        <button class=\"admin-approval-sidebar-card ${state.current && state.current.id === item.id ? 'is-active' : ''}\" data-load-media=\"${escapeHtml(item.id)}\" type=\"button\">\n                            <span class=\"block min-w-0 p-3\">\n                                <span class=\"block truncate font-semibold\">${escapeHtml(item.characterName)}</span>\n                                <span class=\"block truncate text-xs opacity-70\">@${escapeHtml(item.username)} - ${escapeHtml(formatDate(item.createdAt))}</span>\n                                <span class=\"mt-2 flex gap-1\">\n                                    ${item.pendingSfw ? '<span class=\"badge badge-warning badge-xs\">SFW</span>' : ''}\n                                    ${item.pendingNsfw ? '<span class=\"badge badge-warning badge-xs\">NSFW</span>' : ''}\n                                </span>\n                            </span>\n                        </button>\n                    `).join('') : '<p class=\"text-sm text-base-content/60\">No pending approvals.</p>'}\n                </div>\n            </section>\n            <section>\n                <h3 class=\"mb-3 font-bold\">History</h3>\n                <div class=\"grid max-h-[42vh] gap-3 overflow-y-auto pr-1\">\n                    ${state.history.length ? state.history.map((item) => `\n                        <button class=\"admin-approval-sidebar-card ${state.current && state.current.id === item.mediaId ? 'is-active' : ''}\" data-load-media=\"${escapeHtml(item.mediaId)}\" type=\"button\">\n                            <span class=\"block p-3 text-sm\">\n                                <span class=\"flex items-center justify-between gap-2\">\n                                    <span class=\"badge badge-outline\">${escapeHtml(item.imageRating.toUpperCase())}</span>\n                                    <time class=\"text-xs opacity-60\">${escapeHtml(formatDate(item.createdAt))}</time>\n                                </span>\n                                <span class=\"mt-1 block font-semibold\">${escapeHtml(actionLabel(item.action))}</span>\n                                <span class=\"block text-xs opacity-70\">@${escapeHtml(item.ownerUsername)} / ${escapeHtml(item.characterName)}</span>\n                                <span class=\"block text-xs opacity-60\">by @${escapeHtml(item.moderatorUsername)}</span>\n                                <span class=\"mt-2 block text-xs font-semibold text-primary\">Review again</span>\n                            </span>\n                        </button>\n                    `).join('') : '<p class=\"text-sm text-base-content/60\">No historical approvals yet.</p>'}\n                </div>\n            </section>\n        `;\n\n        for (const button of sidebarContainer.querySelectorAll('[data-load-media]')) {\n            button.addEventListener('click', () => loadMedia(button.dataset.loadMedia));\n        }\n    }\n\n    function validateSelections() {\n        if (!state.current) {\n            return {ok: false, error: 'No media is selected.'};\n        }\n\n        if (state.current.sfw && state.current.sfw.needsReview && !selectedActions.sfw) {\n            return {ok: false, error: 'Choose an SFW action.'};\n        }\n\n        if (state.current.nsfw && state.current.nsfw.needsReview && !selectedActions.nsfw) {\n            return {ok: false, error: 'Choose an NSFW action.'};\n        }\n\n        if (selectedActions.sfw === 'mark_nsfw' && state.current.nsfw) {\n            return {ok: false, error: 'Cannot move SFW because this row already has an NSFW image.'};\n        }\n\n        if ((selectedActions.nsfw === 'mark_sfw_homepage' || selectedActions.nsfw === 'mark_sfw_no_homepage') && state.current.sfw) {\n            return {ok: false, error: 'Cannot move NSFW because this row already has an SFW image.'};\n        }\n\n        return {ok: true};\n    }\n\n    async function submitApproval() {\n        const validation = validateSelections();\n\n        if (!validation.ok) {\n            window.alert(validation.error);\n            return;\n        }\n\n        const response = await fetch(`/api/admin/image-approvals/${encodeURIComponent(state.current.id)}`, {\n            method: 'POST',\n            headers: {\n                accept: 'application/json',\n                'content-type': 'application/json',\n                'x-csrf-token': csrfToken,\n            },\n            body: JSON.stringify({\n                sfwAction: selectedActions.sfw,\n                nsfwAction: selectedActions.nsfw,\n            }),\n        });\n        const body = await response.json().catch(() => ({}));\n\n        if (!response.ok) {\n            window.alert(body.error || 'Approval could not be saved.');\n            return;\n        }\n\n        state = body;\n        selectedActions = {};\n        render();\n    }\n\n    function selectShortcutAction(rating, action) {\n        if (!state.current || !state.current[rating]) {\n            return;\n        }\n\n        selectedActions[rating] = action;\n        renderCurrent();\n    }\n\n    function getVisibleVariant(rating) {\n        if (!state.current || sourceWillMove(rating)) {\n            return null;\n        }\n\n        return getMovedVariant(rating) || state.current[rating];\n    }\n\n    function openVariantInNewTab(rating) {\n        const variant = getVisibleVariant(rating);\n\n        if (!variant || !variant.imageUrl) {\n            return;\n        }\n\n        const opened = window.open(variant.imageUrl, '_blank', 'noopener,noreferrer');\n\n        if (opened) {\n            opened.opener = null;\n        }\n    }\n\n    function isKeyboardShortcutTarget(target) {\n        if (!(target instanceof Element)) {\n            return true;\n        }\n\n        return !target.closest('input, textarea, select, button, a, [contenteditable=true]');\n    }\n\n    function handleKeyboardShortcuts(event) {\n        if (event.defaultPrevented || event.ctrlKey || event.metaKey || event.altKey || !isKeyboardShortcutTarget(event.target)) {\n            return;\n        }\n\n        const key = event.key.length === 1 ? event.key.toLowerCase() : event.key;\n        const actionShortcuts = {\n            a: ['sfw', 'approve_sfw_homepage'],\n            s: ['sfw', 'approve_sfw_no_homepage'],\n            d: ['sfw', 'mark_nsfw'],\n            f: ['sfw', 'report_sfw'],\n            j: ['nsfw', 'approve_nsfw'],\n            k: ['nsfw', 'mark_sfw_homepage'],\n            l: ['nsfw', 'mark_sfw_no_homepage'],\n            ';': ['nsfw', 'report_nsfw'],\n        };\n\n        if (key === 'Enter') {\n            event.preventDefault();\n            submitApproval();\n            return;\n        }\n\n        if (key === 'r') {\n            event.preventDefault();\n            openVariantInNewTab('sfw');\n            return;\n        }\n\n        if (key === 'u') {\n            event.preventDefault();\n            openVariantInNewTab('nsfw');\n            return;\n        }\n\n        const shortcut = actionShortcuts[key];\n\n        if (!shortcut) {\n            return;\n        }\n\n        event.preventDefault();\n        selectShortcutAction(shortcut[0], shortcut[1]);\n    }\n\n    async function loadMedia(mediaId) {\n        const response = await fetch(`/api/admin/image-approvals?mediaId=${encodeURIComponent(mediaId)}`, {\n            headers: {\n                accept: 'application/json',\n            },\n        });\n        const body = await response.json().catch(() => ({}));\n\n        if (!response.ok) {\n            window.alert(body.error || 'Media could not be loaded.');\n            return;\n        }\n\n        state = body;\n        selectedActions = {};\n        render();\n    }\n\n    function render() {\n        renderCurrent();\n        renderSidebar();\n    }\n\n    document.addEventListener('keydown', handleKeyboardShortcuts);\n    render();\n})();\n"
