{
  "name": "myoc",
  "type": "module",
  "license": "GPL-3.0-only",
  "packageManager": "npm@11.16.0",
  "engines": {
    "node": ">=24 <25"
  },
  "scripts": {
    "dev": "wrangler types && npm run build && wrangler dev",
    "build": "node scripts/copy-vendor-assets.mjs && tailwindcss -i ./src/styles/app.css -o ./public/app.css --minify",
    "typecheck": "wrangler types && tsc --noEmit",
    "lint": "biome ci .",
    "format": "biome format . --write",
    "deadcode": "npm run deadcode:production && npm run deadcode:nonproduction",
    "deadcode:production": "knip --production --config knip.production.json",
    "deadcode:nonproduction": "knip",
    "containers:check": "npm --prefix containers/myoc-docker-sharp run check",
    "semgrep": "node scripts/run-semgrep.mjs",
    "test": "vitest",
    "test:run": "vitest run",
    "coverage": "vitest run --coverage",
    "coverage:ci": "vitest run --coverage --coverage.reporter=text --coverage.reporter=lcov",
    "ci": "npm run lint && npm run typecheck && npm run coverage:ci && npm run deadcode && npm run containers:check && npm run semgrep",
    "db:prepare:local": "wrangler d1 migrations apply myoc-db --local && wrangler d1 execute myoc-db --local --file seeds/development.sql",
    "env:clone:prod-to-dev": "node scripts/clone-prod-to-dev.mjs",
    "deploy": "npm run build && wrangler deploy --minify"
  },
  "dependencies": {
    "@cloudflare/containers": "^0.3.7",
    "@scure/bip39": "^2.2.0",
    "@simplewebauthn/browser": "^13.3.0",
    "@simplewebauthn/server": "^13.3.2",
    "bcryptjs": "^3.0.3",
    "cropperjs": "^2.1.1",
    "hono": "^4.12.28",
    "openseadragon": "^6.0.2",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.3",
    "@cloudflare/vitest-pool-workers": "^0.18.4",
    "@tailwindcss/cli": "^4.3.2",
    "@vitest/coverage-istanbul": "^4.1.10",
    "daisyui": "^5.6.14",
    "knip": "^6.26.0",
    "tailwindcss": "^4.3.2",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10",
    "wrangler": "^4.108.0"
  },
  "allowScripts": {
    "@parcel/watcher": true,
    "esbuild": true,
    "sharp": true,
    "workerd": true
  }
}
